/* 
    Instalação Global do Newman:
        npm install -g newman

    Módulo de geração de relatório em HTMLAllCollection
        npm install -g newman-reporter-html

    newman run {{caminhoColecaoJson}}

    newman run {{urlColecao}}

    newman run colecaoPostman.json -e testEnv.json
*/